@extends('network.layout')
@section('title','Pagos tenant')
@section('content')<header class="z-page-header"><div><div class="z-eyebrow">ZIGO Network</div><h1>Pagos tenant</h1><p class="z-muted">Observabilidad operativa sin credenciales ni secretos.</p></div></header>
<div class="z-grid">@foreach(['APPROVED'=>'Aprobados','PENDING'=>'Pendientes','REJECTED'=>'Rechazados'] as $key=>$label)<section class="z-metric-card"><span>{{ $label }}</span><strong>{{ $counts[$key] ?? 0 }}</strong></section>@endforeach</div>
<section class="z-card" style="margin-top:1rem"><div class="z-table-wrap"><table class="z-table"><thead><tr><th>Tenant</th><th>Provider</th><th>Entorno</th><th>Estado</th><th>Conectado</th></tr></thead><tbody>@forelse($connections as $connection)<tr><td>{{ $connection->tenant->name }}</td><td>Mercado Pago</td><td>{{ strtoupper(config('zigo_payments.providers.mercado_pago.environment')) }}</td><td><span class="z-badge">{{ $connection->status }}</span></td><td>{{ $connection->connected_at?->format('d/m/Y H:i') ?? '—' }}</td></tr>@empty<tr><td colspan="5">No hay conexiones registradas.</td></tr>@endforelse</tbody></table></div>{{ $connections->links() }}</section>@endsection
