@extends('network.layout') @section('title','Auditoría Network') @section('content')<div class="title">Auditoría administrativa</div><div class="card table-wrap"><table><thead><tr><th>Fecha</th><th>Actor</th><th>Acción</th><th>Entidad</th><th>ID</th><th>Cambio</th></tr></thead><tbody>@forelse($events as $event)<tr><td>{{ $event->created_at?->format('d/m/Y H:i') }}</td><td>{{ $event->actor?->email }}</td><td>{{ $event->action }}</td><td>{{ class_basename($event->entity_type) }}</td><td>{{ $event->entity_id }}</td><td><code>{{ json_encode(['before'=>$event->before_json,'after'=>$event->after_json],JSON_UNESCAPED_UNICODE) }}</code></td></tr>@empty<tr><td colspan="6">Sin acciones administrativas registradas.</td></tr>@endforelse</tbody></table>{{ $events->links() }}</div>@endsection
