@extends('network.layout') @section('title','Suscripciones') @section('content')<div class="title">Suscripciones</div><div class="card table-wrap"><table><thead><tr><th>Empresa</th><th>Plan</th><th>Periodo</th><th>Estado</th><th>Inicio</th><th>Fin</th><th></th></tr></thead><tbody>@forelse($subscriptions as $subscription)<tr><td>{{ $subscription->tenant->name }}</td><td>{{ $subscription->plan->name }}</td><td>{{ $subscription->current_period_start?->format('d/m/Y') }} — {{ $subscription->current_period_end?->format('d/m/Y') }}</td><td><span class="badge badge-{{ $subscription->status }}">{{ strtoupper($subscription->status) }}</span></td><td>{{ $subscription->started_at?->format('d/m/Y') }}</td><td>{{ $subscription->current_period_end?->format('d/m/Y') }}</td><td><a class="btn" href="{{ route('network.subscriptions.show',$subscription) }}">Ver</a></td></tr>@empty<tr><td colspan="7">No hay suscripciones.</td></tr>@endforelse</tbody></table>{{ $subscriptions->links() }}</div>@endsection
