@extends('network.layout') @section('title','Editar módulo') @section('content')
<div class="title">Editar módulo</div><div class="subtitle">Catálogo comercial · separado del estado técnico del Network Map.</div><form method="POST" action="{{ route('network.modules.update',$module) }}">@csrf @method('PUT')<div class="card"><div class="grid"><div class="field"><label>Código</label><input name="code" value="{{ old('code',$module->code) }}" required></div><div class="field"><label>Nombre</label><input name="name" value="{{ old('name',$module->name) }}" required></div><div class="field"><label>Tipo</label><select name="type">@foreach(\App\Domain\Network\Catalog\Models\Module::TYPES as $type)<option value="{{ $type }}" @selected(old('type',$module->type)===$type)>{{ strtoupper($type) }}</option>@endforeach</select></div><div class="field"><label>Orden</label><input name="sort_order" type="number" min="0" value="{{ old('sort_order',$module->sort_order) }}" required></div></div><div class="field"><label>Descripción</label><textarea name="description" rows="3">{{ old('description',$module->description) }}</textarea></div><input type="hidden" name="is_active" value="0"><label><input style="width:auto" type="checkbox" name="is_active" value="1" @checked(old('is_active',$module->is_active))> Disponible en catálogo</label></div><button class="btn">GUARDAR MÓDULO</button></form>
@endsection
