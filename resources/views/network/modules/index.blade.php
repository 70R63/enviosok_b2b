@extends('network.layout')
@section('title','Módulos - ZIGO Network')
@section('content')
<div class="eyebrow">CATÁLOGO COMERCIAL</div><div class="title">Planes y precios</div><nav class="catalog-tabs"><a href="{{ route('network.plans.index') }}">Planes</a><a href="{{ route('network.commercial-products.index') }}">Precios</a><a class="active" href="{{ route('network.modules.index') }}">Módulos</a></nav>
<div class="page-actions"><a class="btn" href="{{ route('network.modules.create') }}">Crear módulo</a></div><div class="card table-wrap"><table><thead><tr><th>Nombre</th><th>Tipo</th><th>Estado</th><th>Orden</th><th>Acción</th></tr></thead><tbody>@forelse($modules as $module)<tr><td><strong>{{ $module->name }}</strong><br><small class="muted">{{ $module->code }}</small></td><td>{{ ucfirst($module->type) }}</td><td><span class="badge badge-{{ $module->is_active?'active':'inactive' }}">{{ $module->is_active?'ACTIVO':'INACTIVO' }}</span></td><td>{{ $module->sort_order }}</td><td><a class="btn btn-secondary" href="{{ route('network.modules.edit',$module) }}">Editar</a></td></tr>@empty<tr><td colspan="5">No hay módulos registrados.</td></tr>@endforelse</tbody></table>{{ $modules->links() }}</div>
@endsection
@push('styles')<style>.catalog-tabs{display:flex;gap:8px;margin:15px 0;border-bottom:1px solid #dbe4ef;padding-bottom:10px}.catalog-tabs a{padding:8px 12px;border-radius:8px;text-decoration:none;color:#475569;font-weight:800}.catalog-tabs a.active{background:#dbeafe;color:#1d4ed8}.page-actions{margin:12px 0}.btn-secondary{background:#e2e8f0;color:#334155}</style>@endpush
