@extends('network.layout') @section('title','Tenants - ZIGO Network') @section('content')
<div class="title">Tenants</div><p><a class="btn" href="{{ route('network.tenants.create') }}">Crear tenant</a></p><div class="card table-wrap"><table><thead><tr><th>Nombre</th><th>Slug</th><th>UUID</th><th>Estado</th><th>Creación</th><th></th></tr></thead><tbody>@forelse($tenants as $tenant)<tr><td>{{ $tenant->name }}</td><td>{{ $tenant->slug }}</td><td>{{ $tenant->uuid }}</td><td>{{ ucfirst($tenant->status) }}</td><td>{{ $tenant->created_at->format('d/m/Y H:i') }}</td><td><a class="btn" href="{{ route('network.tenants.show',$tenant) }}">Ver</a></td></tr>@empty<tr><td colspan="6">No hay tenants registrados.</td></tr>@endforelse</tbody></table>{{ $tenants->links() }}</div>
@endsection
