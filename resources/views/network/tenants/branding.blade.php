@extends('network.layout') @section('title','Branding - '.$tenant->name) @section('content') @php($branding=$tenant->branding)
<div class="eyebrow">WHITE LABEL</div><div class="title">Branding</div><div class="subtitle">{{ $tenant->name }} · PNG, JPG o WEBP; no SVG.</div><form method="POST" enctype="multipart/form-data" action="{{ route('network.tenants.branding.update',$tenant) }}">@csrf @method('PUT')<div class="card"><div class="grid"><div class="field"><label>Nombre comercial</label><input name="brand_name" value="{{ old('brand_name',$branding?->brand_name) }}"></div><div class="field"><label>Color primario</label><input name="primary_color" placeholder="#3B66F6" value="{{ old('primary_color',$branding?->primary_color) }}"></div><div class="field"><label>Color secundario</label><input name="secondary_color" placeholder="#0B2948" value="{{ old('secondary_color',$branding?->secondary_color) }}"></div><div class="field"><label>Color accent</label><input name="accent_color" placeholder="#39A0ED" value="{{ old('accent_color',$branding?->accent_color) }}"></div><div class="field"><label>Email soporte</label><input type="email" name="support_email" value="{{ old('support_email',$branding?->support_email) }}"></div><div class="field"><label>Teléfono soporte</label><input name="support_phone" value="{{ old('support_phone',$branding?->support_phone) }}"></div><div class="field"><label>Logo (máx. 2 MB)</label><input type="file" name="logo" accept="image/png,image/jpeg,image/webp"></div><div class="field"><label>Imagen hero (máx. 4 MB)</label><input type="file" name="hero_image" accept="image/png,image/jpeg,image/webp"></div><div class="field"><label>Favicon (máx. 512 KB)</label><input type="file" name="favicon" accept="image/png,image/jpeg,image/webp"></div></div><button class="btn">GUARDAR BRANDING</button></div></form><p class="muted">Los archivos usan el disco public. Para servirlos localmente puede requerirse <code>php artisan storage:link</code>; no se ejecuta automáticamente.</p>
@endsection
