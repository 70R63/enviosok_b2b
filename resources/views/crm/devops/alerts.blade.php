@extends('devops.layout') @section('title','Alertas - ZIGO DevOps') @section('content')<div class="title">Alertas</div><div class="subtitle">Alertas internas; no se envían notificaciones externas.</div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<section class="card"><div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Severidad</th><th>Tipo</th><th>Ambiente</th><th>Mensaje</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($alerts as $alert)<tr><td>{{ $alert->created_at }}</td><td>{{ $alert->severity }}</td><td>{{ $alert->type }}</td><td>{{ $alert->environment ?: '—' }}</td><td>{{ $alert->message }}</td><td>{{ $alert->status }}</td><td>@if($canAcknowledge && $alert->status==='open')<form method="POST" action="{{ route('devops.alerts.acknowledge',$alert) }}">@csrf<button class="btn btn-small">Reconocer</button></form>@endif</td></tr>@empty<tr><td colspan="7">Sin alertas.</td></tr>@endforelse</tbody></table></div>{{ $alerts->links() }}</section>@endsection
