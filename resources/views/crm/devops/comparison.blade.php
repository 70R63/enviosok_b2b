@extends('devops.layout') @section('title','Comparación - ZIGO DevOps') @section('content')<div class="title">Stage vs PRD</div><div class="subtitle">Comparación basada en releases persistidas.</div><div class="alert alert-info"><strong>{{ $c['status'] }}</strong> — {{ $c['message'] }}</div><div class="summary-grid"><div class="summary-card"><span>Stage</span><strong>{{ $c['stage']?->commit_hash ?: 'Unknown' }}</strong><div class="muted">SHA {{ $c['stage']?->package_sha256 ?: '—' }}</div></div><div class="summary-card"><span>Production</span><strong>{{ $c['production']?->commit_hash ?: 'Unknown' }}</strong><div class="muted">SHA {{ $c['production']?->package_sha256 ?: '—' }}</div></div></div>@endsection
