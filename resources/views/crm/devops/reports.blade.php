@extends('devops.layout') @section('title','Reportes - ZIGO DevOps') @section('content')<div class="title">Reportes</div><form class="card" method="GET"><div class="summary-grid"><label>Ambiente<select name="environment"><option value="">Todos</option><option value="stage">Stage</option><option value="production">Production</option></select></label><label>Estado<input name="status" value="{{ $filters['status'] ?? '' }}"></label><label>Branch<input name="branch" value="{{ $filters['branch'] ?? '' }}"></label><label>Commit<input name="commit" value="{{ $filters['commit'] ?? '' }}"></label><label>Desde<input type="date" name="from" value="{{ $filters['from'] ?? '' }}"></label><label>Hasta<input type="date" name="to" value="{{ $filters['to'] ?? '' }}"></label></div><button class="btn">Filtrar</button></form><div class="summary-grid">@foreach($metrics as $key=>$value)<div class="summary-card"><span>{{ str_replace('_',' ',$key) }}</span><strong>{{ $value }}</strong></div>@endforeach</div>@endsection
