@extends('devops.layout') @section('title',$title.' - ZIGO DevOps') @section('content')<div class="title">{{ $title }}</div><div class="subtitle">Evidencia persistida del Centro de Control.</div><section class="card"><div class="table-wrap"><table><thead><tr>@foreach($headers as $h)<th>{{ $h }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $cell)<td>{{ $cell }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($headers) }}">Sin registros.</td></tr>@endforelse</tbody></table></div></section>@endsection
