<!doctype html><html lang="es"><meta charset="utf-8"><title>Tu envío</title><body><main><h1>Envío {{ $cotizacion->referencia ?: 'ZIGO' }}</h1><dl><dt>Pago</dt><dd>Confirmado</dd><dt>Mensajería</dt><dd>{{ $cotizacion->logistico ?: $cotizacion->carrier }}</dd><dt>Servicio</dt><dd>{{ $cotizacion->servicio ?: $cotizacion->service_code }}</dd><dt>Origen / destino</dt><dd>{{ $cotizacion->ciudad_origen }}, {{ $cotizacion->estado_origen }} → {{ $cotizacion->ciudad_destino }}, {{ $cotizacion->estado_destino }}</dd><dt>Estado de guía</dt><dd>{{ $cotizacion->guia_estatus }}</dd>@if($cotizacion->guia_id)<dt>WayBill</dt><dd>{{ $cotizacion->guia_id }}</dd>@endif @if($cotizacion->tracking_number)<dt>Tracking</dt><dd>{{ $cotizacion->tracking_number }}</dd>@endif</dl>@if($cotizacion->documento)<a href="{{ route('guide-recovery.download',$recoveryToken) }}">Descargar guía</a>@else<p>Estamos recuperando tu guía</p>@endif</main></body></html>
