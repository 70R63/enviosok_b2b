<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><title>{{$incidencia->folio}}</title><style>body{font-family:Arial;background:#f4f7fb;color:#111827;margin:0}.content{max-width:900px;margin:35px auto}.card{background:white;padding:24px;border-radius:18px;box-shadow:0 10px 24px #0001;margin:18px 0}.btn{background:#2563eb;color:white;padding:10px 14px;border:0;border-radius:8px;text-decoration:none;font-weight:800}textarea{width:100%;min-height:100px;box-sizing:border-box;padding:12px;border:1px solid #cbd5e1;border-radius:8px}</style></head><body><main class="content"><a class="btn" href="{{route('b2c.incidencias')}}">← Mis incidencias</a><h1>{{$incidencia->folio}}</h1><section class="card"><b>Estatus:</b> {{$incidencia->estatus}}<br><b>Tipo:</b> {{$incidencia->tipo}}<br><b>Guía:</b> {{$incidencia->tracking_number?:'—'}}<h2>Tu reporte</h2><p>{{$incidencia->customer_message?:$incidencia->descripcion}}</p>@if($incidencia->evidencia)<a class="btn" href="{{route('b2c.incidencias.evidence',$incidencia)}}">Descargar evidencia</a>@endif</section><section class="card"><h2>Respuesta de ZIGO</h2><p>{{$incidencia->public_response?:$incidencia->respuesta_admin?:'Pendiente de revisión'}}</p></section><section class="card"><h2>Conversación</h2>@foreach($incidencia->events as $e)@if($e->public_message)<p><b>{{$e->origin}} · {{$e->created_at?->format('d/m/Y H:i')}}</b><br>{{$e->public_message}}</p>@endif @endforeach @if($incidencia->estatus!=='CERRADA')<form method="POST" action="{{route('b2c.incidencias.comment',$incidencia)}}">@csrf<textarea name="message" required placeholder="Agregar comentario"></textarea><button class="btn">Enviar comentario</button></form>@endif</section></main></body></html>
