@extends('tenant.layout') @section('content') @php($codes=$modules->pluck('code'))
<section class="hero"><div class="eyebrow">{{ $tenant->primaryDomain?->domain??'PREVIEW INTERNO' }}</div><h1>{{ $tenant->branding?->brand_name??$tenant->name }}</h1><p>Plan: <strong>{{ $plan?->name??'Sin plan comercial' }}</strong></p><div class="modules">@forelse($modules as $module)<span class="chip">{{ $module->name }}</span>@empty<span class="notice">Sin módulos activos.</span>@endforelse</div></section>
<section class="actions">@if($codes->contains('B2C')||$codes->contains('SHIPPING'))<article class="action"><b>Cotizar</b><span>Vista preliminar. Cotización real todavía no conectada.</span></article>@endif @if($codes->contains('TRACKING'))<article class="action"><b>Tracking</b><span>Vista preliminar. Seguimiento real todavía no conectado.</span></article>@endif @if($codes->contains('CRM'))<article class="action"><b>Soporte</b><span>Vista preliminar. Atención real todavía no conectada.</span></article>@endif</section><p class="notice">Preview White Label · No ejecuta operaciones logísticas.</p>
@endsection
