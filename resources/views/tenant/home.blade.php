@extends('tenant.layout') @section('content') @php($codes=$modules->pluck('code'))
<section class="hero"><div class="eyebrow">{{ $tenant->primaryDomain?->domain??'PREVIEW INTERNO' }}</div><h1>{{ $tenant->branding?->brand_name??$tenant->name }}</h1><p>Plan: <strong>{{ $plan?->name??'Sin plan comercial' }}</strong></p><div class="modules">@forelse($modules as $module)<span class="chip">{{ $module->name }}</span>@empty<span class="notice">Sin módulos activos.</span>@endforelse</div></section>
<section class="actions">@if($codes->contains('B2C')&&$codes->contains('SHIPPING'))<a class="action" href="{{ route('tenant.b2c.quote.create') }}"><b>Cotizar</b><span>Consulta opciones logísticas y precio final.</span></a>@endif @if($codes->contains('B2C')&&$codes->contains('TRACKING'))<a class="action" href="{{ route('tenant.b2c.tracking') }}"><b>Tracking</b><span>Entrada controlada de seguimiento.</span></a>@endif @if($codes->contains('CRM'))<article class="action"><b>Soporte</b><span>Integración CRM tenant-aware pendiente.</span></article>@endif</section>
@endsection
