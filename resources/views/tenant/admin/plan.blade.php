@extends('tenant.admin.layout')
@section('title','Mi plan')
@section('content')<div class="eyebrow">CONFIGURACIÓN COMERCIAL · SÓLO LECTURA</div><h1>Mi plan</h1><section class="card"><h2>{{ $tenant->currentPlan?->name??'Sin plan comercial' }}</h2>@if($tenant->currentPlan)<p><strong>{{ $tenant->currentPlan->code }}</strong> · {{ $tenant->currentPlan->included_operations??'Sin límite definido' }} operaciones incluidas</p><h3>Servicios contratados</h3><div class="chips">@forelse($tenant->currentPlan->modules as $module)<span class="chip">{{ $module->name }}</span>@empty<span class="muted">Este plan no incluye módulos.</span>@endforelse</div>@endif<p class="muted">Los cambios de plan se gestionan desde ZIGO Network. Subscription y Entitlements se incorporarán en ZN-04.</p></section>@endsection
