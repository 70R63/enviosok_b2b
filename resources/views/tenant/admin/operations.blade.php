@extends('tenant.admin.layout')
@section('title','Operaciones')
@section('content')<div class="eyebrow">OPERACIÓN</div><h1>Operaciones</h1><div class="card table-wrap"><table><thead><tr><th>UUID</th><th>Fecha</th><th>Estado</th><th>Servicio</th><th>Provider</th><th>Referencia</th><th></th></tr></thead><tbody>@forelse($operations as $operation)<tr><td title="{{ $operation->uuid }}"><code>{{ $operation->uuid }}</code></td><td>{{ $operation->created_at->format('d/m/Y H:i') }}</td><td>{{ strtoupper($operation->status) }}</td><td>{{ $operation->service_code ?? '—' }}</td><td>{{ $operation->provider ?? '—' }}</td><td>{{ $operation->external_reference ?? '—' }}</td><td>@if($operation->status==='quoted')<form method="POST" action="{{ route('tenant.admin.operations.confirm',$operation->uuid) }}">@csrf<button class="btn">Confirmar</button></form>@endif</td></tr>@empty<tr><td colspan="7">Sin operaciones.</td></tr>@endforelse</tbody></table></div>{{ $operations->links() }}@endsection
