@php($branding=$tenant?->branding)
<!doctype html><html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Restablecer acceso · {{ $branding?->brand_name ?? $tenant?->name }}</title><link rel="stylesheet" href="{{ asset('css/zigo-auth.css') }}"></head><body><div class="zigo-auth-container"><main class="zigo-auth-card zigo-auth-form"><h1>Restablece tu acceso</h1><p class="zigo-auth-description">Escribe el correo asociado a tu cuenta. Si existe una cuenta asociada, enviaremos instrucciones de acceso.</p>@if(session('status'))<div class="zigo-auth-alert zigo-auth-alert-success">{{ session('status') }}</div>@endif @if($errors->any())<div class="zigo-auth-alert zigo-auth-alert-error">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('tenant.admin.password.email', [], false) }}">@csrf<label class="zigo-auth-group">Correo electrónico<input type="email" name="email" value="{{ old('email') }}" required autofocus autocomplete="email"></label><button class="zigo-auth-button">Enviar instrucciones</button></form><div class="zigo-auth-links"><a href="{{ route('tenant.admin.login', [], false) }}">Volver a iniciar sesión</a></div></main></div></body></html>
