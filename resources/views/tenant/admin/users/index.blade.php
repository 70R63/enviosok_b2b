@extends('tenant.admin.layout')
@section('title','Usuarios')
@section('content')<div class="eyebrow">TENANT MEMBERSHIPS</div><h1>Usuarios</h1><section class="card table-wrap"><table><thead><tr><th>NOMBRE</th><th>EMAIL</th><th>ROL TENANT</th><th>ESTADO</th><th>ACCIÓN</th></tr></thead><tbody>@foreach($memberships as $item)<tr><td>{{ $item->user->name }}</td><td>{{ $item->user->email }}</td><td colspan="3"><form method="POST" action="{{ route('tenant.admin.users.update',$item) }}" style="display:grid;grid-template-columns:130px 130px auto;gap:8px">@csrf @method('PATCH')<select name="role">@foreach(\App\Domain\Network\Tenancy\Models\TenantMembership::ROLES as $role)<option value="{{ $role }}" @selected($item->role===$role)>{{ strtoupper($role) }}</option>@endforeach</select><select name="status">@foreach(\App\Domain\Network\Tenancy\Models\TenantMembership::STATUSES as $status)<option value="{{ $status }}" @selected($item->status===$status)>{{ strtoupper($status) }}</option>@endforeach</select><button class="btn">GUARDAR</button></form></td></tr>@endforeach</tbody></table></section>@endsection
