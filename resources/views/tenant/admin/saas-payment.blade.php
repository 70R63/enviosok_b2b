@extends('tenant.admin.layout')
@section('title','Pago de servicio ZIGO')
@section('content')<div style="max-width:680px;margin:auto"><section class="z-card z-stack"><div class="z-eyebrow">Tenant → ZIGO</div><h1>{{ $order->purchase_snapshot['name'] }}</h1><div class="z-metric-card"><span>Total</span><strong>${{ number_format((float)$order->total_amount,2) }} {{ $order->currency }}</strong></div><p>La activación ocurre únicamente después de una aprobación verificada.</p>@if($order->status==='ACTIVATED')<div class="z-alert z-alert--success">Servicio activado.</div>@elseif(config('zigo_payments.platform.enabled'))<form method="POST" action="{{ route('tenant.admin.saas.checkout',$order->uuid) }}">@csrf<button class="z-btn z-btn--block">Pagar con Mercado Pago</button></form>@else<div class="z-alert">Los pagos SaaS aún no están configurados.</div>@endif</section></div>@endsection
