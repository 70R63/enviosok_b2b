@extends('tenant.layout')
@section('title','Cotizar · '.($tenant->branding?->brand_name ?: $tenant->name))
@section('content')
<header class="z-page-header"><div><div class="z-eyebrow">NUEVO ENVÍO</div><h1>Cotiza tu envío</h1><p class="z-muted">Conoce los servicios disponibles y su precio antes de continuar.</p></div></header>
<span class="z-sr-only">CP origen</span>
@include('tenant.b2c.partials.quote-form', ['customerPortal' => $customerPortal ?? false])
@if($result)<section class="customer-section"><div class="z-eyebrow">RESULTADOS</div><h2>Elige cómo enviar</h2><div class="quote-result-grid">@forelse($result['options'] as $index=>$option)<article class="z-card quote-result"><div><span class="z-badge">{{ $option['provider'] ?? 'ZIGO Local' }}</span><h3>{{ $option['service'] }}</h3><p class="z-muted">{{ $option['sla'] ?? 'Servicio disponible' }}</p></div><dl><div><dt>Origen</dt><dd>{{ $option['origin'] ?? '—' }}</dd></div><div><dt>Destino</dt><dd>{{ $option['destination'] ?? '—' }}</dd></div></dl><strong class="quote-result__price">${{ number_format((float)$option['price'],2) }} <small>{{ $option['currency'] }}</small></strong><form method="POST" action="{{ $customerPortal ?? false ? route('tenant.customer.app.quote.select',[],false) : route('tenant.b2c.quote.select',[],false) }}">@csrf<input type="hidden" name="operation_uuid" value="{{ $result['operation']->uuid }}"><input type="hidden" name="option" value="{{ $index }}"><button class="z-btn z-btn--block">Seleccionar servicio</button></form></article>@empty<div class="z-empty"><h3>No hay tarifas disponibles</h3><p>Revisa los datos o intenta más tarde.</p></div>@endforelse</div></section>@endif
@push('head')<style>.quote-result-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:1rem}.quote-result{display:flex;flex-direction:column;gap:1rem}.quote-result h3{margin:.7rem 0 .2rem}.quote-result dl{display:grid;gap:.7rem;margin:0}.quote-result dl div{padding:.7rem;border-radius:10px;background:var(--z-surface-subtle)}.quote-result dt{font-size:.75rem;color:var(--z-muted);text-transform:uppercase}.quote-result dd{margin:.15rem 0 0;font-weight:700}.quote-result__price{font-size:1.6rem;color:var(--z-primary);margin-top:auto}.quote-result__price small{font-size:.8rem}</style>@endpush
@endsection
