@php($branding=$tenant->branding)
@php($brand=$branding?->brand_name ?: $tenant->name)
@php($safeColor=fn($value,$fallback)=>is_string($value)&&preg_match('/^#[0-9A-Fa-f]{6}$/',$value)?$value:$fallback)
@php($logoUrl=$branding?->logo_path && Storage::disk('public')->exists($branding->logo_path) ? route('tenant.branding.logo',[],false) : null)
<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title',$brand)</title>
<link rel="stylesheet" href="{{ asset('css/zigo-design-system.css') }}"><style>:root{--z-primary:{{ $safeColor($branding?->primary_color,'#185FA8') }};--z-primary-dark:{{ $safeColor($branding?->secondary_color,'#0B2948') }};--z-accent:{{ $safeColor($branding?->accent_color,'#39A0ED') }}}body{min-height:100vh;background:linear-gradient(145deg,color-mix(in srgb,var(--z-primary-dark) 94%,#000),var(--z-primary));margin:0}.guest-shell{min-height:100vh;display:grid;grid-template-rows:auto 1fr auto}.guest-header{padding:20px clamp(20px,5vw,56px);display:flex;align-items:center;justify-content:space-between}.guest-brand{display:flex;align-items:center;gap:12px;color:#fff;text-decoration:none;font-weight:900;font-size:1.15rem}.guest-brand img{max-width:170px;max-height:52px;object-fit:contain}.guest-mark{width:42px;height:42px;border-radius:12px;background:#fff;color:var(--z-primary);display:grid;place-items:center}.guest-home{color:#fff;font-weight:800;text-decoration:none}.guest-main{display:grid;place-items:center;padding:24px}.auth-shell{width:min(100%,480px)}.auth-shell .z-card{padding:clamp(24px,5vw,38px);box-shadow:0 28px 70px rgba(0,0,0,.24)}.guest-footer{text-align:center;color:rgba(255,255,255,.75);padding:20px}</style></head><body><div class="guest-shell"><header class="guest-header"><a class="guest-brand" href="{{ route('tenant.customer.landing',[],false) }}">@if($logoUrl)<img src="{{ $logoUrl }}" alt="{{ $brand }}">@else<span class="guest-mark">{{ mb_strtoupper(mb_substr($brand,0,1)) }}</span><span>{{ $brand }}</span>@endif</a><a class="guest-home" href="{{ route('tenant.customer.landing',[],false) }}">← Volver a Inicio</a></header><main class="guest-main">@yield('content')</main><footer class="guest-footer">{{ $brand }}</footer></div></body></html>
