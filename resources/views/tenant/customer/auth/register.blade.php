@extends('tenant.layout')
@section('title','Crear cuenta · '.($tenant->branding?->brand_name ?: $tenant->name))
@section('content')<div class="auth-shell"><div class="z-card z-stack"><div><div class="z-eyebrow">NUEVA CUENTA</div><h1>Crea tu cuenta</h1><p class="z-muted">Tu identidad se asociará únicamente a {{ $tenant->branding?->brand_name ?: $tenant->name }}.</p></div>@if($errors->any())<div class="z-alert z-alert--danger" role="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="/registro" class="z-stack">@csrf<label class="z-field">Nombre<input name="name" value="{{ old('name') }}" autocomplete="name" required></label><label class="z-field">Email<input type="email" name="email" value="{{ old('email') }}" autocomplete="email" required></label><label class="z-field">Contraseña<input type="password" name="password" autocomplete="new-password" required></label><label class="z-field">Confirmar contraseña<input type="password" name="password_confirmation" autocomplete="new-password" required></label><label class="z-choice"><input type="checkbox" name="terms" value="1" required><span>Acepto los términos y el aviso de privacidad aplicables.</span></label><button class="z-btn z-btn--block">Crear cuenta</button></form><p>¿Ya tienes cuenta? <a href="/login">Inicia sesión</a></p></div></div>@endsection
