@extends('tenant.customer.auth.layout')
@section('title','Iniciar sesión · '.($tenant->branding?->brand_name ?: $tenant->name))
@section('content')<div class="auth-shell"><div class="z-card z-stack"><div><div class="z-eyebrow">MI CUENTA</div><h1>Iniciar sesión</h1><p class="z-muted">Accede a tus envíos de {{ $tenant->branding?->brand_name ?: $tenant->name }}.</p></div>@if($errors->any())<div class="z-alert z-alert--danger" role="alert">Correo o contraseña incorrectos.</div>@endif<form method="POST" action="{{ route('tenant.customer.login.store',[],false) }}" class="z-stack">@csrf<label class="z-field">Email<input type="email" name="email" value="{{ old('email') }}" autocomplete="email" required autofocus></label><label class="z-field">Contraseña<input type="password" name="password" autocomplete="current-password" required></label><label class="z-choice"><input type="checkbox" name="remember" value="1"><span>Mantener sesión iniciada</span></label><button class="z-btn z-btn--block">Iniciar sesión</button></form><div class="z-cluster" style="justify-content:space-between"><a href="/forgot-password">¿Olvidaste tu contraseña?</a><a href="{{ route('tenant.customer.register',[],false) }}">Crear cuenta</a></div></div></div>@endsection
