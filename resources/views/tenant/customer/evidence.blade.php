@extends('tenant.layout')
@section('title','Opciones de entrega')
@section('content')<header class="z-page-header"><div><div class="z-eyebrow">EVIDENCIA</div><h1>Opciones de entrega</h1><p class="z-muted">Sólo se muestran modalidades activas para esta operación.</p></div></header><div class="z-grid">@forelse($options as $option)<article class="z-card"><h2>{{ $option->name }}</h2><p class="z-muted">{{ $option->description }}</p><div class="z-cluster">@if($option->require_signature)<span class="z-badge">Firma</span>@endif @if($option->require_photo)<span class="z-badge">Foto</span>@endif @if($option->require_gps)<span class="z-badge">Ubicación</span>@endif @if($option->require_receiver_name)<span class="z-badge">Nombre de quien recibe</span>@endif</div>@if((float)$option->surcharge_amount>0)<p><strong>Recargo foundation: ${{ number_format($option->surcharge_amount,2) }} {{ $option->currency }}</strong></p><p class="z-help">El total comercial final se integrará con checkout en una fase posterior.</p>@endif</article>@empty<div class="z-empty"><h3>Sin modalidades disponibles</h3><p>No hay opciones de evidencia activas en este momento.</p></div>@endforelse</div>@endsection
